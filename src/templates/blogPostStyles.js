const getStyles = (theme) =>({
  blogContainer: { 
    textAlign: 'center',
    backgroundColor: "#2a2e2b",
    color: "#fff",
    height: "100vh"
  },
  title: {
    paddingTop: 40
  },
  img: {
    borderRadius: "5%"
  }
   
})

export default getStyles